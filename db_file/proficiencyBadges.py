from website.models import *

new_proficiencyBadges = [
                        ProficiencyBadge(
                            member_id="member_id",
                            name="ProficiencyBadge_name",
                            pass_date="ProficiencyBadge_pass_date"
                        )
                    ]